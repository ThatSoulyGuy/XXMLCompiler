# XXML Compiler macOS Code Signing Configuration
# Copy this file to .env.local and fill in your credentials
# NEVER commit .env.local to version control!

# =============================================================================
# Apple Developer Account Credentials
# =============================================================================

# Your Apple Developer Team ID (10-character string)
# Find it at: https://developer.apple.com/account -> Membership -> Team ID
APPLE_TEAM_ID=XXXXXXXXXX

# Your Apple ID email address
APPLE_ID=your-email@example.com

# App-specific password for notarization
# Create one at: https://appleid.apple.com -> Security -> App-Specific Passwords
APPLE_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# =============================================================================
# Code Signing Identities (from Keychain)
# =============================================================================

# Developer ID Application certificate identity
# Used for signing the binaries themselves
# List available identities: security find-identity -v -p codesigning
# Format: "Developer ID Application: Your Name (TEAMID)"
DEVELOPER_ID_APPLICATION="Developer ID Application: Your Company Name (XXXXXXXXXX)"

# Developer ID Installer certificate identity
# Used for signing the PKG installer package
# Format: "Developer ID Installer: Your Name (TEAMID)"
DEVELOPER_ID_INSTALLER="Developer ID Installer: Your Company Name (XXXXXXXXXX)"

# =============================================================================
# Keychain Configuration (optional)
# =============================================================================

# Path to keychain containing signing certificates (optional)
# Leave empty to use default login keychain
# KEYCHAIN_PATH=/path/to/signing.keychain-db

# Keychain password (only needed if using a custom keychain)
# KEYCHAIN_PASSWORD=

# =============================================================================
# Notarization Configuration
# =============================================================================

# Primary bundle ID for notarization
NOTARIZATION_BUNDLE_ID=com.xxml.compiler

# Wait for notarization to complete (true/false)
# If false, will submit and return immediately
NOTARIZATION_WAIT=true

# Timeout for notarization in seconds (default: 3600 = 1 hour)
NOTARIZATION_TIMEOUT=3600

# =============================================================================
# Build Configuration
# =============================================================================

# Product version (leave empty to read from CMakeLists.txt)
# XXML_VERSION=3.0.0

# Output directory for installer (relative to installer/)
OUTPUT_DIR=out

# Include debug symbols in release build (true/false)
INCLUDE_DEBUG_SYMBOLS=false

# =============================================================================
# Optional: CI/CD Specific Settings
# =============================================================================

# Base64-encoded p12 certificate for CI (optional)
# Used when running in CI environments without Keychain access
# Generate with: base64 -i certificate.p12 | tr -d '\n'
# CERTIFICATE_P12_BASE64=

# Password for the p12 certificate
# CERTIFICATE_P12_PASSWORD=
